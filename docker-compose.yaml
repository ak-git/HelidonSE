services:
  helidon-se:
    container_name: helidon-se
    image: a002k/helidon-se:latest
    ports:
      - "8080:8080"
    networks:
      - bridge-net
    volumes:
      - helidon-se:/home/app-user/

networks:
  bridge-net:
    driver: bridge

volumes:
  helidon-se: